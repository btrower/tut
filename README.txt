Tutorial files

Note -- this was set up to push to github.

On Windows workstations it is helpful to have
the program git-credential-winstore.exe in the
directory where the git command is installed. 

On 'Standard Directory System', Cygwin is 
typically here:

%HOMEDRIVE%\std\cyg\bin

